body {
  font-family: 'Segoe UI', sans-serif;
  background: #f3f6fc;
  margin: 0;
  padding: 0;
  color: #333;
}

.container {
  max-width: 700px;
  margin: 50px auto;
  background: white;
  padding: 30px;
  border-radius: 12px;
  box-shadow: 0 8px 30px rgba(0, 0, 0, 0.1);
}

.header {
  display: flex;
  align-items: center;
  gap: 10px;
}

.logo {
  width: 40px;
  height: 40px;
}

.hidden {
  display: none;
}

#welcome input {
  padding: 10px;
  width: 70%;
  margin-top: 10px;
}

#welcome button {
  padding: 10px 20px;
  margin-top: 10px;
}

#chatbox {
  margin-top: 20px;
  max-height: 400px;
  overflow-y: auto;
  padding: 10px;
  background: #f9f9f9;
  border-radius: 10px;
  border: 1px solid #ccc;
}

.message {
  display: flex;
  align-items: flex-start;
  margin-bottom: 15px;
}

.message.user {
  flex-direction: row-reverse;
  text-align: right;
}

.message .avatar {
  width: 40px;
  height: 40px;
  border-radius: 50%;
  margin: 0 10px;
}

.message .text {
  max-width: 70%;
  background: #e0f0ff;
  padding: 10px 15px;
  border-radius: 15px;
  line-height: 1.5;
}

.message.user .text {
  background: #dcf8c6;
}

.input-area {
  display: flex;
  margin-top: 10px;
  gap: 10px;
}

.input-area input {
  flex: 1;
  padding: 12px;
  border-radius: 8px;
  border: 1px solid #ccc;
}

.input-area button {
  padding: 12px 20px;
  background: #007bff;
  color: white;
  border: none;
  border-radius: 8px;
  cursor: pointer;
}

.input-area button:hover {
  background: #0056b3;
}

.contact {
  margin-top: 20px;
  font-size: 0.9em;
  text-align: center;
}

.typing .dots {
  font-size: 24px;
  animation: blink 1s infinite steps(3, jump-none);
}

.typing .dots span {
  opacity: 0;
  animation: blinkDot 1.5s infinite;
}

.typing .dots span:nth-child(1) { animation-delay: 0s; }
.typing .dots span:nth-child(2) { animation-delay: 0.2s; }
.typing .dots span:nth-child(3) { animation-delay: 0.4s; }

@keyframes blinkDot {
  0%, 100% { opacity: 0; }
  50% { opacity: 1; }
}
